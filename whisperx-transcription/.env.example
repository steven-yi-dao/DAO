# Copy to .env.local and fill in from the CloudFormation stack outputs:
#   sam list stack-outputs --stack-name whisperx
# Until all three are set the SPA keeps running against its built-in mocks.

VITE_API_URL=
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
