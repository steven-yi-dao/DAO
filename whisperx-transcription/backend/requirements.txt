# whisperx / faster-whisper / ctranslate2 are tightly coupled and are the most
# common source of breakage — do not bump one without re-testing the set on the
# exact base image.
whisperx==3.1.5
faster-whisper==1.0.3
ctranslate2==4.4.0

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# torch and torchaudio come from the base image.
# pyannote.audio is intentionally absent: diarization is not supported.
